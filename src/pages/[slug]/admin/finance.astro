---
import AdminLayout from "../../../layouts/AdminLayout.astro";
import AdminMangasBreadcrumb from "../../../components/admin/AdminMangasBreadcrumb";
import AdminFinance from "../../../components/admin/AdminFinance";

// @ts-ignore - organization se define dinámicamente en el middleware
const { organization, user, organizationSlug } = Astro.locals;
const logged = !!user;

// Si no hay organización, redirigir a 404
if (!organization) {
  return Astro.redirect("/404");
}

if (!logged) {
  return Astro.redirect(`/${organizationSlug}/login`);
}
---

<AdminLayout title="Finanzas" page="finance" organizationSlug={organizationSlug}>
  <AdminMangasBreadcrumb organization={organization} organizationSlug={organizationSlug} client:only="react"/>
  <div class="py-2"></div>
  <h1>Finanzas</h1>
  <AdminFinance organization={organization} organizationSlug={organizationSlug} client:only="react"/>
</AdminLayout>

