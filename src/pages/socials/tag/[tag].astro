---
// Las etiquetas de la comunidad ahora viven en La Charca.
const { tag } = Astro.params;
const t = String(tag || '').replace(/^#/, '').toLowerCase();
return Astro.redirect(t ? `https://lacharca.com/tag/${encodeURIComponent(t)}` : 'https://lacharca.com/', 301);
---
